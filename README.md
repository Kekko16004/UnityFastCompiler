# UnityFC
Possibility to decide when to have Unity compile, avoiding blocking the main thread at the slightest change

# Installation
Go inyour project's folder, Assets>Editor> paste them this you scripts (create if not existthe "Editor" folder, Unity knows)
